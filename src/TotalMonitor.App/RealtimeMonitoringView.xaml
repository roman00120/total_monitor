<UserControl x:Class="TotalMonitor.App.RealtimeMonitoringView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:TotalMonitor.App">
  <StackPanel>
    <DockPanel Margin="0,0,0,16">
      <TextBlock Text="Monitoreo en tiempo real" FontSize="20" FontWeight="SemiBold"/>
      <Border DockPanel.Dock="Right" Background="#F4F7FA" CornerRadius="12" Padding="12,4" BorderBrush="#CBD2D9" BorderThickness="1">
        <TextBlock Text="{Binding SimulatorIndicator}" Foreground="{Binding IndicatorColor}" FontWeight="Bold" FontSize="12"/>
      </Border>
    </DockPanel>

    <!-- Cards Grid -->
    <ItemsControl ItemsSource="{Binding LiveCards}">
      <ItemsControl.ItemsPanel>
        <ItemsPanelTemplate>
          <WrapPanel/>
        </ItemsPanelTemplate>
      </ItemsControl.ItemsPanel>
      <ItemsControl.ItemTemplate>
        <DataTemplate DataType="{x:Type local:LiveMetricCard}">
          <Border Background="White" CornerRadius="8" BorderBrush="#E2E8F0" BorderThickness="1" Margin="0,0,14,14" Width="220" Padding="16">
            <StackPanel>
              <TextBlock Text="{Binding Variable}" Foreground="#627D98" FontWeight="SemiBold" FontSize="13"/>
              <StackPanel Orientation="Horizontal" Margin="0,8,0,4">
                <TextBlock Text="{Binding Value}" FontSize="24" FontWeight="Bold" Foreground="#102A43"/>
                <TextBlock Text="{Binding Unit}" FontSize="14" Foreground="#486581" FontWeight="SemiBold" VerticalAlignment="Bottom" Margin="6,0,0,3"/>
              </StackPanel>
              <TextBlock Text="{Binding Timestamp, StringFormat='Actualizado: {0}'}" FontSize="11" Foreground="#9FB3C8"/>
            </StackPanel>
          </Border>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>

    <!-- Empty/Status Banner if no cards -->
    <Border Style="{StaticResource PanelStyle}" Margin="0,10,0,0">
      <StackPanel>
        <TextBlock Text="ESTADO DEL FLUJO DE DATOS" Foreground="#627D98" FontWeight="Bold" FontSize="12"/>
        <TextBlock Text="{Binding EmptyMessage}" FontSize="15" Margin="0,10,0,0" Foreground="#243B53" TextWrapping="Wrap"/>
        <TextBlock Text="{Binding ChartMessage}" Foreground="#627D98" FontSize="13" Margin="0,8,0,0"/>
      </StackPanel>
    </Border>
  </StackPanel>
</UserControl>
