<Window x:Class="TotalMonitor.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:TotalMonitor.App"
        Title="TOTAL MONITOR" Width="1280" Height="760" MinWidth="960" MinHeight="620"
        Background="{StaticResource BackgroundBrush}">
  <Window.Resources>
    <Style x:Key="SidebarButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
      <Setter Property="Foreground" Value="#EAF2F8"/>
      <Setter Property="Background" Value="Transparent"/>
      <Setter Property="FontSize" Value="14"/>
      <Setter Property="FontWeight" Value="Normal"/>
      <Setter Property="Height" Value="44"/>
      <Setter Property="Padding" Value="16,0"/>
      <Setter Property="Margin" Value="0,3"/>
      <Setter Property="HorizontalContentAlignment" Value="Left"/>
      <Setter Property="BorderThickness" Value="0"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border Background="{TemplateBinding Background}" CornerRadius="3" Padding="{TemplateBinding Padding}">
              <TextBlock Text="{TemplateBinding Content}"
                         VerticalAlignment="Center"
                         HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                         Foreground="{TemplateBinding Foreground}"
                         FontSize="{TemplateBinding FontSize}"
                         FontWeight="{TemplateBinding FontWeight}"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.45"/></Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
      <Style.Triggers>
        <Trigger Property="IsMouseOver" Value="True">
          <Setter Property="Background" Value="#243B53"/>
          <Setter Property="Foreground" Value="White"/>
        </Trigger>
        <Trigger Property="IsPressed" Value="True">
          <Setter Property="Background" Value="#16B8A6"/>
          <Setter Property="Foreground" Value="White"/>
        </Trigger>
      </Style.Triggers>
    </Style>
    <DataTemplate DataType="{x:Type local:DashboardViewModel}"><local:DashboardView/></DataTemplate>
    <DataTemplate DataType="{x:Type local:MetersViewModel}"><local:MetersView/></DataTemplate>
    <DataTemplate DataType="{x:Type local:RealtimeMonitoringViewModel}"><local:RealtimeMonitoringView/></DataTemplate>
    <DataTemplate DataType="{x:Type local:HistoricalViewModel}"><local:HistoricalView/></DataTemplate>
    <DataTemplate DataType="{x:Type local:UsersViewModel}"><local:UsersView/></DataTemplate>
    <DataTemplate DataType="{x:Type local:ReportsViewModel}"><local:ReportsView/></DataTemplate>
    <DataTemplate DataType="{x:Type local:SettingsViewModel}"><local:SettingsView/></DataTemplate>
  </Window.Resources>
  <Grid>
    <Grid.RowDefinitions><RowDefinition Height="72"/><RowDefinition Height="*"/><RowDefinition Height="34"/></Grid.RowDefinitions>
    <Border Grid.Row="0" Background="White" Padding="28,0">
      <DockPanel>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
          <Image Source="/Assets/Logo/Total-Monitor.png" Width="46" Height="46" Stretch="Uniform" Margin="0,0,14,0"/>
          <StackPanel VerticalAlignment="Center">
          <TextBlock Text="TOTAL MONITOR" FontSize="22" FontWeight="Bold"/>
          <TextBlock Text="POWER QUALITY PLATFORM" FontSize="10" Foreground="#627D98"/>
          </StackPanel>
        </StackPanel>
        <TextBlock DockPanel.Dock="Right" Text="● SISTEMA OPERATIVO" Foreground="#138A72" VerticalAlignment="Center" FontWeight="SemiBold"/>
      </DockPanel>
    </Border>
    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions><ColumnDefinition Width="220"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
      <Border Background="{StaticResource SidebarBrush}">
        <StackPanel Margin="16,28">
          <Button Style="{StaticResource SidebarButtonStyle}" Content="▦   Dashboard" Command="{Binding DashboardCommand}"/>
          <Button Style="{StaticResource SidebarButtonStyle}" Content="▤   Medidores" Command="{Binding MetersCommand}"/>
          <Button Style="{StaticResource SidebarButtonStyle}" Content="◉   Monitoreo" Command="{Binding MonitoringCommand}"/>
          <Button Style="{StaticResource SidebarButtonStyle}" Content="◷   Históricos" Command="{Binding HistoryCommand}"/>
          <Button Style="{StaticResource SidebarButtonStyle}" Content="▣   Reportes" Command="{Binding ReportsCommand}"/>
          <Button Style="{StaticResource SidebarButtonStyle}" Content="⚙   Configuración" Command="{Binding SettingsCommand}"/>
        </StackPanel>
      </Border>
      <ScrollViewer Grid.Column="1" Padding="34" VerticalScrollBarVisibility="Auto">
        <StackPanel>
          <DockPanel Margin="0,0,0,24">
            <StackPanel>
              <TextBlock Text="{Binding CurrentSection}" FontSize="30" FontWeight="Bold"/>
              <TextBlock Text="Monitoreo centralizado de dispositivos" Foreground="#627D98"/>
            </StackPanel>
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
              <Button Content="Iniciar adquisición" Command="{Binding StartCommand}" Background="#16B8A6" Width="150"/>
              <Button Content="Detener" Command="{Binding StopCommand}" Foreground="#243B53" Width="90"/>
            </StackPanel>
          </DockPanel>
          <ContentControl Content="{Binding CurrentViewModel}"/>
        </StackPanel>
      </ScrollViewer>
    </Grid>
    <Border Grid.Row="2" Background="#E5ECF2" Padding="24,0">
      <DockPanel>
        <TextBlock Text="{Binding Dashboard.AcquisitionState}" VerticalAlignment="Center"/>
        <TextBlock DockPanel.Dock="Right" Text="{Binding Dashboard.OperationMode}" Foreground="#627D98" VerticalAlignment="Center"/>
      </DockPanel>
    </Border>
  </Grid>
</Window>
